I want you to make an ASR model for the language of Rengmitca (glottology code reng1255) under the assumption that we initially know nothing about the language, i.e. a zero-shot asr model. From here, the models that give the best results, as well as the data that is closest to what I want will be correct, and then used to fine-tune a future model based on the ones that give the best results in the initial pass, so there will always be that supervised learning. 

Currently, what I have in relation to data is independently spoken words, meaning that I don't need to worry about overlapping audio messing with the asr model. 
I currently have 4, 40-minute blocks of audio recordings that contains Bengali (female speaker), mru (male speaker_1) and rengmitca (male speaker_2), but fot testing, I will be using a 5 minute .wav file to speed up the initial testing, only later will I use the full 40 minute block. 

The pipeline that I would like as such is; first a diarisation model (there are 3 speakers total, one woman and two men, but for my testing thus far, I have not been able to get models to distinguish between the two men, but that is okay, as long as there is a distinguish between the men and the woman, since that is one less thing I have to worry about); then I would like to have it segmented to allow me to listen to each word independently, and such that the IPA transcription can be on the specific segmentation; then each segment in IPA transcription in the form of a .txt file (this may change later depending on what I need); then lastly a tone pass, to test to see if there are definitively tones, and what tones the model thinks exists in the language. You an always check the file ".\pipeline.txt" if needs be
Additionally, within each model, there is a verification process (which can be seen in "cd .\verification_%d" depending on which stage of the pipeline I am in), this is to allow me to see what the model is doing, and if I need to, it will allow me to make minor edits, such as the edits which on speaker-diarization-3.1 where i can change the number of speakers through the following command "diarization = pipeline("audio.wav", num_speakers=2)" or if i want to lower the increase the lowerbound of Hz because there is a lot of noise, or if i want to increate the amount of time above a certain Hz to allow any coughs to not be recognised.
As such that the final pipeline looks like this:
diarisation -> verification_1 -> segmentation -> verification_2 -> ipa -> verification_3 -> tone -> verification_4
Very important, I already have the folders made, so any code that you write pertaining to a specific model, write it within the folder, and then the output given by the model will be in the "verification

There are some models that may allow you to all of that at once, for example "VibeVoice ASR" which can diarise and segment, as well as provide a transcription. All the models that I want to run from hugginface can be found at "https://huggingface.co/collections/juanpalm/ipa-transcriber", and ".\HF-models.txt" additionally you have my HF_TOKEN through os.getenv(), and my GH_TOKEN os.getenv(). 

I want you to document everything that you have done. Additionally I want you to push this to my GitHub under a new repository called "ipa-transcriber-04" after every trial, commit to GitHub. Are there any questions?